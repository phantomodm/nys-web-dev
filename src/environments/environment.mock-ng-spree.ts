import { DEFAULT_CONFIG } from '../config/default/default';

export const environment = {
  production: false,
  apiEndpoint: 'https://nys-web-dev-new.herokuapp.com/',
  appName: DEFAULT_CONFIG.appName,
  config: DEFAULT_CONFIG
};
